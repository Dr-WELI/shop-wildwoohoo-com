# WildWooHooShop

The repository for **shop.wildwoohoo.com**, the online shop for WildWooHoo Studio.

Built as a Jekyll site, deployed via GitHub Pages. GitHub Pages auto-builds on push, no GitHub Action required.

- **Main site:** [wildwoohoo.com](https://wildwoohoo.com)
- **Shop domain:** shop.wildwoohoo.com (this repo)
- **Owner:** Dr WELI / WildWooHoo Studio (ABN 61 239 662 726)

## Architecture in one paragraph

Layouts in `_layouts/`, partials in `_includes/`, brand and nav data in `_data/`, categories as a Jekyll collection in `_categories/`, products (when they land) as a Jekyll collection in `_products/`. Update one file, the change propagates to every page. CSS is split between brand tokens (`css/wwh-brand-tokens.css`, a verbatim mirror of the main site) and shop-local styles (`css/shop.css`).

## Repo layout

```
WildWooHooShop/
├── _config.yml                     Jekyll config: plugins, collections, defaults
├── Gemfile                         pins github-pages gem
├── CNAME                           shop.wildwoohoo.com
├── .gitignore                      excludes _site, .jekyll-cache
├── README.md                       this file (excluded from build)
├── STRATEGY-SUMMARY.md             excluded from build
├── TODO.md                         excluded from build
│
├── _data/
│   ├── site.yml                    brand, chip, launch plan, social, AoC, email
│   ├── nav.yml                     primary + studio + footer legal nav
│   ├── books.yml                   shared book-line data (imprint, companion, ethics)
│   ├── policies.yml                ALL policy copy: returns, shipping, tax, ACL, children
│   └── interest.yml                register-interest hooks: labels, subjects, promise
│
├── _layouts/
│   ├── default.html                base shell (head + header + main + footer)
│   ├── category.html               category layout: hero, status, auto product grid
│   ├── product.html                generic product layout, extends default
│   └── book.html                   book / book-combo product layout
│
├── _includes/
│   ├── head.html                   <head>: meta, OG, favicons, schema, fonts, CSS
│   ├── header.html                 <header>: brand chip + nav + mobile menu + cart
│   ├── footer.html                 <footer>: menus + AoC + legal row
│   ├── category-card.html          card for the landing-page grid
│   ├── product-card.html           card for category auto-grids, honest availability chip
│   ├── buy-state.html              the single buy panel: price, tax, state, CTA
│   ├── launch-timeline.html        landing opening-plan strip (from site.yml launch)
│   ├── register-interest.html      one-email capture hook (from interest.yml)
│   ├── trust-strip.html            landing identity strip: who, ships, returns, prices
│   ├── policy-strip.html           returns / delivery / your-rights strip on product pages
│   ├── product-meta.html           generic spec grid for non-book products
│   ├── related-products.html       manual cross-sell (page.related, max 3)
│   ├── safety-note.html            children's age + safety line (page.age_band)
│   ├── book-meta.html              book spec grid
│   ├── book-combo-contents.html    slipcase contents list
│   ├── book-companion.html         free-companion explainer
│   └── ethics-transparency.html    production ethics + ICIP block
│
├── _categories/                    category collection (Jekyll iterates this)
│   ├── childrens-books.html
│   ├── educational.html
│   ├── wear.html
│   ├── prints.html
│   └── tools.html
│
├── _products/                      product collection, builds at /products/[slug]/
│   ├── kangaroo-time-across-seasons-set.html
│   └── a-kangaroo-mob-in-summer.html
│
├── _template/
│   ├── product-page.html           reference template (excluded from build)
│   ├── book-product.html           book-line reference template (excluded from build)
│   └── README.md                   how to add a new product + front-matter contract
│
├── index.html                      landing: hero, timeline, categories, interest, trust
├── policies.html                   /policies/ destination page (renders policies.yml)
├── 404.html                        404, served by GitHub Pages
├── robots.txt
│
├── css/
│   ├── wwh-brand-tokens.css        verbatim mirror of main site tokens
│   └── shop.css                    shop-local styles
│
└── assets/
    └── img/                        favicons, og-shop.png, brand chip; product
                                    imagery goes here too, one folder per product
```

`sitemap.xml` is auto-generated by the `jekyll-sitemap` plugin at build time. Do not commit it.

## How to make common changes

### Add a new category

Create `_categories/[slug].html` with front matter and content body. Reference any existing file (e.g., `_categories/wear.html`) for the schema. Jekyll generates the page at `/categories/[slug]/` and the landing-page card grid auto-includes it. Set `interest_hook` to a hook id from `_data/interest.yml` and the register-interest band renders at the foot of the page.

### Add a new product

1. Copy `_template/product-page.html` content (or `_template/book-product.html` for the book line) into `_products/[product-slug].html`.
2. Replace every `REPLACE-ME` placeholder in the front matter and body.
3. Set `category` to one of the five category slugs (`childrens-books`, `educational`, `wear`, `prints`, `tools`) and give it an `order`. The product then appears on its category page automatically; the category layout builds its grid from `site.products | where: 'category', page.slug`.
4. Drop 7 product images into `/assets/img/[product-slug]/` (`01-hero.jpg` through `07-model.jpg` - see `_template/README.md` for the order rationale, per shop immersion Insight 6).
5. The page builds at `/products/[product-slug]/` automatically.

### Make a product buyable (the buy-state contract)

Every product page renders the buy panel (`_includes/buy-state.html`). Two front-matter keys drive it:

- `availability`: `live` | `in_stock` | `preorder` | `out_of_stock`. A missing key renders the preorder state.
- `checkout_url`: a Lemon Squeezy hosted-checkout URL. With `availability: live` it renders a real Buy now link; the default layout loads lemon.js on that page so the link opens as an overlay, and the plain link still works without JS. Without `checkout_url`, `live` shows a disabled "Cart opening soon" button.

`fulfilment` (`digital` | `pod` | `inventory`) picks the tax line and the delivery disclosure line. No scarcity counters, no urgency timers; the include enforces the honest-state discipline.

### Change policy wording (returns, shipping, tax, children)

Edit `_data/policies.yml`. It is the single source for every policy sentence: `/policies/`, the policy strip on product pages, the trust strip on the landing, the buy panel's tax and delivery lines, and the category-page trust line all render from it. Wording cannot diverge because it lives once.

### Change the register-interest hooks

Edit `_data/interest.yml`. The landing band and every category page render their hooks from it (button label, mailto subject, context line, the one-email promise). When a list platform lands (SHP-007), swap the mailto for a form embed in `_includes/register-interest.html` once; no page changes.

### Change the opening dates

Edit the `launch:` block in `_data/site.yml`. The landing hero meta and the opening timeline both render from it.

### Change the nav

Edit `_data/nav.yml`. Every page's header and footer nav updates, including the footer legal row (Returns / Shipping / Privacy).

### Change the brand metadata (email, AoC, social, etc.)

Edit `_data/site.yml`. Every page that references that field updates.

### Change the header or footer markup

Edit `_includes/header.html` or `_includes/footer.html`. Every page updates.

## Local development

Requires Ruby 3.1+ (the system Ruby on older macOS may be too old).

```bash
cd ~/Studio/Websites/WildWooHooShop
bundle install
bundle exec jekyll serve
# Site runs at http://localhost:4000
```

If you don't have a modern Ruby installed locally, that's fine — push to GitHub and Pages will build it. Build status: https://github.com/Dr-WELI/shop-wildwoohoo-com/actions or the deploy tab in the repo.

## Recommended phased launch (see STRATEGY-SUMMARY.md)

This scaffold is intentionally backend-agnostic. The recommended path:

- **Phase 1 (Q3 2026):** open with digital downloads, use **Lemon Squeezy** embedded buy buttons on this static scaffold. AU$0/mo fixed cost. Lemon Squeezy is the merchant-of-record so AU GST + EU VAT + US sales tax are handled for you.
- **Phase 2 (Q4 2026):** add POD merch via **Shopify Lite** (AU$9/mo) + Printful Australia. Embed Shopify Buy Buttons on this scaffold.
- **Phase 3 (2027, only when justified):** migrate to **Shopify Basic** (AU$45/mo) when SKUs > 15, or limited drops live, or revenue > AU$3K/mo.

For books specifically, **Lulu Direct** (Shopify-native book POD) lets you sell hardcover and paperback from this storefront, printed and shipped on demand, keeping your imprint. See the books-pathway notes in your conversation history.

## DNS + GitHub Pages (already configured 2026-06-08)

These were the one-off setup steps WELI ran on 2026-06-08. Documented here for posterity.

1. Repo: `Dr-WELI/shop-wildwoohoo-com` (public).
2. GitHub Pages: enabled, source = main branch / root, custom domain = `shop.wildwoohoo.com`, HTTPS enforced.
3. DNS: CNAME record `shop` → `dr-weli.github.io` at Cloudflare, proxy status "DNS only" (grey cloud), TTL Auto.
4. Cert: Let's Encrypt, auto-renews.

## What's intentionally NOT here

| Not here | Why |
|---|---|
| Cart, checkout, payment | Phase 1 will use embedded Lemon Squeezy. Phase 2 will use Shopify Lite Buy Buttons. Neither wired yet. |
| Real product copy | Products do not exist yet. Placeholders only. |
| Analytics pixels | Get added when traffic is enough to read, not before. |
| Music releases | Live on Bandcamp (planned: welimusic.bandcamp.com). |
| Sync EPK | Lives on wildwoohoo.com/music-and-entertainment/. |
| Programme bookings | Live on drweli.com (cockpit / direct). |
| Newsletter signup | Defer until first 50 subscribers ask for it, then Buttondown. |
| Indigenous-art-inspired products | Out of scope without documented ICIP consultation. See main `~/Studio/CLAUDE.md`. |

## Discipline this repo inherits

From `~/Studio/CLAUDE.md` and `~/Studio/Websites/CLAUDE.md`:

- **Brand-domain rule.** WildWooHoo content lives on `wildwoohoo.com` and `shop.wildwoohoo.com`; Dr WELI content lives on `drweli.com`. Don't cross the streams.
- **AU English.** behaviour, programme, centre, organise, realise, colour.
- **Anti-AI-tell.** No em-dashes. No curly quotes. No Furthermore / Moreover / Additionally / In conclusion / Importantly / leverage / robust / delve / tapestry.
- **No overclaim.** No "show-stopping", "vanguard", "next big thing".
- **No re-introducing struck terms.** "Brain·Body·Planet" stays struck. "Living Wild" stays struck as a framing word.
- **Existing copy is good.** Lean on what the strategy docs already say. Don't rewrite.
- **No live-site changes** without WELI's explicit go.

## Reading list for future sessions

| Where | What |
|---|---|
| `STRATEGY-SUMMARY.md` (this repo) | Distilled shop strategy |
| `TODO.md` (this repo) | Next concrete steps |
| `~/Studio/Strategy-Hub-2026/01_WildWooHoo-Studio/SHOP-PRODUCTS-PIPELINE-PLAN.md` | The full 51KB shop strategy doc |
| `~/Studio/Industry-Knowledge/immersion/shop/insights.md` | 35 product-page conventions to apply |
| `~/Studio/Websites/WildWooHooDotCom/brand-kit/README.md` | Brand kit v5 |
| `~/Studio/Websites/WildWooHooDotCom/wwh-brand-tokens.css` | Canonical palette tokens |
| `~/Studio/Strategy-Hub-2026/01_WildWooHoo-Studio/_LOCKED-DECISIONS-2026-06-04.md` | Locked decisions parent doc |
| `~/Code/wildwoohoo-shop/README.md` | The Shopify management-layer app (port 3025) |
| `~/Code/wildwoohoo-books-companion/README.md` | The companion PWA app |
